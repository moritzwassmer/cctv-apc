_target_: napc.model.NAPC
defaults:
- base_model
- input: dense_baseline
- intermediate: xlstm
- output: dense

# Overwrite the embedding_dim for all components
input:
  embedding_dim: 128
intermediate:
  config:
    embedding_dim: 128
output:
  embedding_dim: 128